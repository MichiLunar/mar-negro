import 'package:flutter/material.dart';

class BeginningView extends StatelessWidget {
  const BeginningView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.green);
  }
}
